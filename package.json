{
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "eslint": "^8.16.0",
    "husky": "^8.0.1",
    "jest": "^28.1.0",
    "prettier": "^2.6.2",
    "ts-jest": "^28.0.2"
  },
  "scripts": {
    "test": "jest --config='./jest-config/jest.config.ts'",
    "prepare": "husky install"
  }
}
